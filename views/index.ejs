<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Consulta de Datos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #2c3e50; /* Azul medianoche */
      color: #ecf0f1; /* Gris claro */
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .container {
      background-color: #34495e; /* Gris oscuro para el contenedor */
      border-radius: 10px;
      padding: 30px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    }
    h1 {
      color: #3498db; /* Azul vibrante para el título */
      text-align: center;
      margin-bottom: 30px;
    }
    .form-control {
      background-color: #4a657c;
      color: #ecf0f1;
      border: 1px solid #5a7796;
    }
    .form-control:focus {
      background-color: #5a7796;
      color: #ecf0f1;
      border-color: #3498db;
      box-shadow: 0 0 0 0.25rem rgba(52, 152, 219, 0.25);
    }
    .input-group .btn-primary {
      background-color: #3498db; /* Azul primario */
      border-color: #3498db;
      transition: all 0.3s ease;
    }
    .input-group .btn-primary:hover {
      background-color: #2980b9;
      border-color: #2980b9;
      transform: translateY(-2px);
    }
    .btn-secondary {
      background-color: #7f8c8d; /* Gris concreto */
      border-color: #7f8c8d;
      transition: all 0.3s ease;
    }
    .btn-secondary:hover {
      background-color: #95a5a6;
      border-color: #95a5a6;
      transform: translateY(-2px);
    }
    .btn-primary { /* Para el botón de crear nuevo usuario */
      background-color: #27ae60; /* Verde esmeralda */
      border-color: #27ae60;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #2ecc71;
      border-color: #2ecc71;
      transform: translateY(-2px);
    }
  </style>
</head>
<body class="container mt-5">
  <h1 class="mb-4">Consulta de Datos desde Google Sheets</h1>

  <form action="/mostrar-todos" method="GET" class="mb-4">
    <div class="input-group">
      <input type="text" name="q" class="form-control" placeholder="Buscar por nombre, ID u otro campo">
      <button class="btn btn-primary" type="submit">Buscar</button>
    </div>
  </form>
<div class="mb-3 d-flex justify-content-between">
  <a href="/mostrar-todos" class="btn btn-secondary">Mostrar todos</a>

  <a href="/crear" class="btn btn-primary">
    <i class="bi bi-plus-lg"></i> Crear nuevo usuario
  </a>
</div>

</body>
</html>
