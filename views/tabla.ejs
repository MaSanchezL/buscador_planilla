<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Listado de Usuarios</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #2c3e50; /* Azul medianoche */
      color: #ecf0f1; /* Gris claro */
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .container {
      background-color: #34495e; /* Gris oscuro para el contenedor principal */
      border-radius: 10px;
      padding: 30px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    }
    h1 {
      color: #3498db; /* Azul vibrante para el título */
      text-align: center;
      margin-bottom: 30px;
    }
    .btn-outline-primary {
      color: #3498db;
      border-color: #3498db;
      transition: all 0.3s ease;
    }
    .btn-outline-primary:hover {
      background-color: #3498db;
      color: #fff;
      transform: translateY(-2px);
    }
    .alert-warning {
      background-color: #f39c12; /* Naranja para la alerta */
      color: #fff;
      border-color: #e67e22;
    }
    .card {
      background-color: #4a657c; /* Un azul un poco más claro para las tarjetas */
      color: #ecf0f1;
      border: none;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5);
    }
    .card-title {
      color: #3498db; /* Azul vibrante para el título de la tarjeta */
      font-weight: bold;
    }
    .card-text strong {
      color: #ecf0f1;
    }
    .btn-primary {
      background-color: #27ae60; /* Verde esmeralda para el botón de detalle */
      border-color: #27ae60;
      transition: all 0.3s ease;
    }
    .btn-primary:hover {
      background-color: #2ecc71;
      border-color: #2ecc71;
      transform: translateY(-2px);
    }
  </style>
</head>
<body class="container mt-5">
  <h1 class="mb-4">Usuarios</h1>
  <a href="/" class="btn btn-outline-primary mb-4">Volver</a>

  <% if (datos.length === 0) { %>
    <div class="alert alert-warning">No hay datos para mostrar.</div>
  <% } else { %>
    <div class="row row-cols-1 row-cols-md-3 g-4">
      <% datos.forEach(d => { %>
        <div class="col">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title"><%= d.nombre %></h5>
              <p class="card-text">App: <strong><%= d.app %></strong></p>
              <a href="/detalle/<%= d.id %>" class="btn btn-primary">Ver detalle</a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  <% } %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
